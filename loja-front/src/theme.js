import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  spacing: 8, // Define a função spacing
  // Adicione outras configurações de tema aqui, se necessário
});

export default theme;
